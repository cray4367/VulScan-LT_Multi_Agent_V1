# Core dependencies
#datasets>=2.14.0
#only if working on benchvul else datasets is not required
groq>=0.5.0
openai>=1.0.0
scikit-learn>=1.3.0
pandas>=2.0.0
tqdm>=4.65.0
streamlit>=1.30.0
plotly>=5.18.0

# ────────────────────────────────────────────────────────────────────────────
# RAG dependencies (optional) - only if you re-enable RAG in rag/cwe_index.py
# faiss-cpu>=1.7.4     (~50 MB)
# transformers>=4.35.0 (~500 MB - 1 GB)
# torch>=2.0.0         (~800 MB - 2 GB)
#
# To install RAG dependencies: pip install faiss-cpu transformers torch
# See rag/cwe_index.py for re-enabling instructions.
# ────────────────────────────────────────────────────────────────────────────
