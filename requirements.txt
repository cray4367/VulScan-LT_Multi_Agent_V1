# Core dependencies
#datasets>=2.14.0
#only if working on benchvul else datasets is not required
groq>=0.5.0
openai>=1.0.0
scikit-learn>=1.3.0
pandas>=2.0.0
tqdm>=4.65.0
streamlit>=1.30.0
#plotly>=5.18.0
#not required since we removed benchvul plotting
